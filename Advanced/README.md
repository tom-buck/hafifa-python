# Advanced Topics

כשמתקדמים בפייתון, יש דברים יותר עמוקים שחשוב להבין. כאן תמצאו נושאים מתקדמים שיעזרו לכם לכתוב קוד יותר טוב ויותר מקצועי.

### מעט רקע

#### פייתון כשפה עילית (High-Level)

פייתון היא עילית, זאת אומרת שהיא קריאה ופשוטה. אבל תכלס מוסתר מאיתנו כול הניהול הזכרון המורכב ודרך המדוייקת שהמחשב עושה את הדברים. 
אנחנו נהנים מהפשטה של הדברים.

המשפט הכי חשוב שצריך להכיר כדי להבין את עומק השפה הוא:
> **"Everything in Python is an Object"**

זה אומר שכל דבר – מספרים, מחרוזות, פונקציות ואפילו מחלקות עצמן – הם אובייקטים.

ניגע בזאת עוד בהמשך , אך חשוב שתכירו

#### Dunder Methods

בפייתון ישנם פונקציות נסתרות , שנקראות Dunder Methods.
(Dunder - Double Underscore)
עד כה כנראה נתקלת בפונקציה __init__ - פונקציה שקובעת מה קורה באתחול של מחלקה.

אז קיימות הרבה פונקציות כאלה שהן מגדירות התנהגות של אובייקטים - במקרה של __init__ - התנהגות של יצירת אובייקט במחלקה.

ההתנהגות של אובייקטים בפייתון מוגדרת באמצעות הפונקציות האלו 

שם מוגדר מה קורה תכלס כאשר קוראים דברים מסויים , למשל : 

* **חיבור (+):** כשאתם כותבים `a + b`, פייתון למעשה קוראת לפונקציה `a.__add__(b)`.
* **אורך (len):** כשאתם בודקים אורך של רשימה `len(my_list)`, פייתון קוראת ל-`my_list.__len__()`.
* **הדפסה (print):** כשמדפיסים אובייקט, פייתון מחפשת את `__str__` כדי לדעת איך להציג אותו כטקסט.



#### למה זה חשוב לנו?

זה מאפשר לנו להבין יותר לעומק אובייקטים מסויימים בשפה , ולשנות ולחקות אותם כדי לעשות דברים כפי שנרצה


### שלב 1: Generators and Iterators

פירוט בקובץ: [Generators and Iterators](./generators_and_iterators.md)

---

### שלב 2: Decorators

פירוט בקובץ: [Decorators](./decorators.md)

---

### שלב 3: Context Managers

פירוט בקובץ: [Context Managers](./context_managers.md)

---

### שלב 4: Python Internals and Descriptors

פירוט בקובץ: [Python Internals and Descriptors](./internals_and_descriptors.md)

---

### שלב 5: Meta Classes

פירוט בקובץ: [Meta Classes](./meta_classes.md)

---
